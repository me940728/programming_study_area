/*
자바 코딩의 핵심은 명료성(clarity)와 단순성(simplicity) 이다. 코드는 복사가 아니라 재사용 되어야 한다.
- 자바 8버전의 언어 명세에 따르면
자바 지원 타입(type, 자료형)은 Interface, Class, Array, Primitive(기본형) 총 네가지 이다.
Annotation 은 인터페이스의 일종이며 Enum(열거)형은 클래스 일종이다.
Interface, Class, Array 는 Reference type 이다.
- 클래스의 인스턴스와 배열은 객체(Object)이지만 기본형은 객체가 아니다.
- Class 의 Member 는 필드, 메서드, 멤버클래스, 멤버인터페이스가 있다.
- 메서드의 시그니처는 메서드의 이름 매개변수의 타입들로 이루어진다(반환값의 타입은 시그니처에 포함되지 않는다.)
- 상속과 서브클래싱은 동일 의미, 아무것도 명시하지 않는 access level 은 package-private 를 쓴다.
- API 는 직렬화된 형태(Serialized form)을 말한다. API 제공자를 user 사용자를 client 라고 한다.
- 공개 API 는 API 정의한 패키지 밖에서 접근할 수 있는 API 요소로 이루어진다.
- 자바9 부터 모듈시스템이라는 개념이 추가 되었다.
* */